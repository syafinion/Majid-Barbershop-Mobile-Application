import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MajidBarbershop3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
